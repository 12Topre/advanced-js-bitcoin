class BitcoinPurchaseFrequency {
    constructor(id, purchaseFrequency) {
      this.id = id;
    
      this.purchaseFrequency = purchaseFrequency;
    }

    minimumFractionDigits: 2
});
return formatter.format(this.purchaseFrequency);
}
} 

/* Not sure about this one, I guessed */ 